{
  "name": "starmoor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check": "node scripts/check-boundaries.mjs",
    "test": "vitest run",
    "build": "npm run check && tsc --noEmit && vitest run && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "trystero": "^0.21.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
